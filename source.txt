var
    a, b, c: logical;
    s: string;
begin
    write("Enter a: ");
    read(a);
    write("Enter b: ");
    read(b);
    c = true;
    s = "hello world";

    if !(a | b) & c then
        writeln("yeah");
    else begin
        write("nope");
        write("...");
    end

    writeln(s);
end
