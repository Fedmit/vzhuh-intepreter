var
    a, b, c, r: logical;
    s: string;
begin
    write("Enter a: ");
    read(a);
    write("Enter b: ");
    read(b);
    c = true;
    s = "hello world";

    r = !(a | b) & c;

    writeln(s);
    writeln(r);
end
